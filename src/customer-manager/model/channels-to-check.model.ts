import {LinkInterface} from "../../model/link/link.interface";
import {ChannelToCheckModel} from "./channel-to-check.model";

export interface ChannelsToCheckModel {
    channelsToCheck : ChannelToCheckModel[]
}